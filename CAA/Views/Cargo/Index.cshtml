@model IEnumerable<CAA.Models.Cargo>

@{
    ViewData["Title"] = "Cargos";
}

<style>

    /* Page Header */
    .cargos-page-header {
        background: linear-gradient(135deg, rgba(37, 211, 102, 0.05) 0%, rgba(18, 140, 126, 0.05) 100%);
        border-radius: 16px;
        padding: 2rem;
        margin-bottom: 2rem;
        border: 1px solid rgba(37, 211, 102, 0.2);
        backdrop-filter: blur(10px);
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .cargos-header-content h1 {
        font-size: 1.75rem;
        font-weight: 700;
        color: var(--caa-green-dark);
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

        .cargos-header-content h1 i {
            font-size: 2rem;
        }

    .cargos-header-content p {
        color: #6b7280;
        margin: 0;
        font-size: 0.95rem;
    }

    .btn-new-cargo {
        background: linear-gradient(135deg, var(--caa-green-main), var(--caa-green-dark));
        color: white;
        border: none;
        border-radius: 12px;
        padding: 0.875rem 1.75rem;
        font-weight: 600;
        font-size: 0.95rem;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
        box-shadow: 0 4px 15px rgba(37, 211, 102, 0.2);
    }

        .btn-new-cargo:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(37, 211, 102, 0.35);
            color: white;
        }

    /* List Container */
    .cargos-list-container {
        background: white;
        border-radius: 20px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        border: 2px solid #f3f4f6;
    }

    .cargos-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .cargo-item {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1.5rem 2rem;
        border-bottom: 1px solid #f3f4f6;
        transition: all 0.3s ease;
        gap: 1rem;
    }

        .cargo-item:last-child {
            border-bottom: none;
        }

        .cargo-item:hover {
            background: var(--caa-green-light);
        }

    .cargo-info {
        display: flex;
        align-items: center;
        gap: 1rem;
        flex: 1;
        min-width: 0;
    }

    .cargo-icon {
        width: 48px;
        height: 48px;
        background: var(--caa-green-light);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

        .cargo-icon i {
            font-size: 1.5rem;
            color: var(--caa-green-main);
        }

    .cargo-name {
        font-size: 1.1rem;
        font-weight: 600;
        color: #111b21;
        margin: 0;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .cargo-actions {
        display: flex;
        gap: 0.5rem;
        flex-shrink: 0;
    }

    .btn-action {
        padding: 0.5rem 0.875rem;
        border-radius: 10px;
        border: none;
        font-size: 0.85rem;
        font-weight: 600;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        text-decoration: none;
        cursor: pointer;
    }

    .btn-edit {
        background: rgba(37, 211, 102, 0.1);
        color: var(--caa-green-main);
    }

        .btn-edit:hover {
            background: var(--caa-green-main);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3);
        }

    .btn-details {
        background: rgba(59, 130, 246, 0.1);
        color: #3b82f6;
    }

        .btn-details:hover {
            background: #3b82f6;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
        }

    .btn-delete {
        background: rgba(239, 68, 68, 0.1);
        color: #ef4444;
    }

        .btn-delete:hover {
            background: #ef4444;
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(239, 68, 68, 0.3);
        }

    /* Empty State */
    .empty-state {
        text-align: center;
        padding: 5rem 2rem;
        color: #6b7280;
    }

        .empty-state i {
            font-size: 5rem;
            color: #d1d7db;
            margin-bottom: 1.5rem;
        }

        .empty-state h3 {
            color: #374151;
            margin-bottom: 0.75rem;
            font-size: 1.5rem;
            font-weight: 700;
        }

        .empty-state p {
            color: #9ca3af;
            font-size: 1rem;
            margin-bottom: 1.5rem;
        }

    /* Responsive */
    @@media (max-width: 768px) {
        .cargos-page-header

    {
        flex-direction: column;
        align-items: stretch;
        text-align: center;
    }

    .cargos-header-content h1 {
        justify-content: center;
    }

    .btn-new-cargo {
        width: 100%;
        justify-content: center;
    }

    .cargo-item {
        flex-direction: column;
        align-items: stretch;
        gap: 1rem;
    }

    .cargo-info {
        flex-direction: column;
        text-align: center;
    }

    .cargo-actions {
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn-action {
        flex: 1;
        min-width: 100px;
        justify-content: center;
    }

    }
</style>

<div class="container-fluid px-3">
    <!-- Page Header -->
    <div class="cargos-page-header">
        <div class="cargos-header-content">
            <h1>
                <i class="bi bi-briefcase"></i>
                <span>Cargos</span>
            </h1>
            <p>Gerencie os cargos disponíveis nas empresas parceiras</p>
        </div>
        <a asp-action="Create" class="btn-new-cargo">
            <i class="bi bi-plus-circle"></i>
            <span>Novo Cargo</span>
        </a>
    </div>

    <!-- Cargos List -->
    <div class="cargos-list-container">
        @if (Model != null && Model.Any())
        {
            <ul class="cargos-list">
                @foreach (var cargo in Model)
                {
                    <li class="cargo-item">
                        <div class="cargo-info">
                            <div class="cargo-icon">
                                <i class="bi bi-briefcase-fill"></i>
                            </div>
                            <h3 class="cargo-name">@Html.DisplayFor(modelItem => cargo.Nome)</h3>
                        </div>
                        <div class="cargo-actions">
                            <a asp-action="Edit" asp-route-id="@cargo.CargoId" class="btn-action btn-edit" title="Editar">
                                <i class="bi bi-pencil"></i>
                                <span>Editar</span>
                            </a>
                            <a asp-action="Details" asp-route-id="@cargo.CargoId" class="btn-action btn-details" title="Detalhes">
                                <i class="bi bi-eye"></i>
                                <span>Ver</span>
                            </a>
                            <a asp-action="Delete" asp-route-id="@cargo.CargoId" class="btn-action btn-delete" title="Excluir">
                                <i class="bi bi-trash"></i>
                                <span>Excluir</span>
                            </a>
                        </div>
                    </li>
                }
            </ul>
        }
        else
        {
            <div class="empty-state">
                <i class="bi bi-briefcase"></i>
                <h3>Nenhum cargo cadastrado</h3>
                <p>Cadastre o primeiro cargo</p>
                <a asp-action="Create" class="btn-new-cargo">
                    <i class="bi bi-plus-circle"></i>
                    <span>Cadastrar Primeiro Cargo</span>
                </a>
            </div>
        }
    </div>
</div>

<!--início::Cabeçalho-->
<nav class="app-header navbar navbar-expand bg-body">
    <!--início::Container-->
    <div class="container-fluid">
        <!--início::Links da Navbar (Esquerda)-->
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link sidebar-toggle-btn" data-lte-toggle="sidebar" href="#" role="button">
                    <i class="bi bi-list"></i>
                </a>
            </li>
            <li class="nav-item">
                <a asp-controller="Home" asp-action="Index" class="nav-link nav-link-custom">
                    <i class="bi bi-house-door"></i>
                    <span class="d-none d-md-inline">Home</span>
                </a>
            </li>
            <li class="nav-item">
                <a asp-controller="Home" asp-action="Sobre" class="nav-link nav-link-custom">
                    <i class="bi bi-info-circle"></i>
                    <span class="d-none d-md-inline">Sobre</span>
                </a>
            </li>
        </ul>
        <!--fim::Links da Navbar (Esquerda)-->
        <!--início::Links da Navbar (Direita)-->
        <ul class="navbar-nav ms-auto">
            <!--início::Alternar Tela Cheia-->
            <li class="nav-item">
                <a class="nav-link fullscreen-toggle-btn" href="#" data-lte-toggle="fullscreen">
                    <i data-lte-icon="maximize" class="bi bi-arrows-fullscreen"></i>
                    <i data-lte-icon="minimize" class="bi bi-fullscreen-exit" style="display: none;"></i>
                </a>
            </li>
            @await Component.InvokeAsync("UserProfile")
        </ul>
        <!--fim::Links da Navbar (Direita)-->
    </div>
    <!--fim::Container-->
</nav>
<!--fim::Cabeçalho-->

<style>
    /* ===================== TOP NAVBAR STYLING ===================== */

    /* Main Header */
    .app-header {
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(249, 250, 251, 0.98) 100%) !important;
        border-bottom: 2px solid rgba(37, 211, 102, 0.15) !important;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04) !important;
        backdrop-filter: blur(10px);
        padding: 0.5rem 0;
        transition: all 0.3s ease;
        position: sticky;
        top: 0;
        z-index: 1030;
    }

        /* Container Adjustments */
        .app-header .container-fluid {
            padding: 0 1.5rem;
        }

    /* ===================== SIDEBAR TOGGLE BUTTON ===================== */

    .sidebar-toggle-btn {
        width: 42px;
        height: 42px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        background: transparent;
        margin-right: 0.5rem;
    }

        .sidebar-toggle-btn i {
            font-size: 1.5rem;
            color: #6b7280;
            transition: all 0.3s ease;
        }

        .sidebar-toggle-btn:hover {
            background: var(--caa-green-light);
            transform: scale(1.05);
        }

            .sidebar-toggle-btn:hover i {
                color: var(--caa-green-main);
            }

    /* ===================== NAVIGATION LINKS ===================== */

    .nav-link-custom {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.625rem 1rem !important;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.95rem;
        color: #374151 !important;
        transition: all 0.3s ease;
        position: relative;
        margin: 0 0.25rem;
    }

        .nav-link-custom i {
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .nav-link-custom span {
            position: relative;
        }

        /* Hover Effect */
        .nav-link-custom:hover {
            background: var(--caa-green-light);
            color: var(--caa-green-dark) !important;
            transform: translateY(-2px);
        }

            .nav-link-custom:hover i {
                color: var(--caa-green-main);
                transform: scale(1.1);
            }

        /* Active State */
        .nav-link-custom.active {
            background: linear-gradient(135deg, var(--caa-green-main), var(--caa-green-dark));
            color: white !important;
            box-shadow: 0 4px 12px rgba(37, 211, 102, 0.3);
        }

            .nav-link-custom.active i {
                color: white;
            }

        /* Underline Animation */
        .nav-link-custom::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 3px;
            background: var(--caa-green-main);
            border-radius: 2px;
            transition: width 0.3s ease;
        }

        .nav-link-custom:hover::after {
            width: 70%;
        }

        .nav-link-custom.active::after {
            display: none;
        }

    /* ===================== FULLSCREEN BUTTON ===================== */

    .fullscreen-toggle-btn {
        width: 42px;
        height: 42px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
        background: transparent;
        margin-left: 0.5rem;
    }

        .fullscreen-toggle-btn i {
            font-size: 1.25rem;
            color: #6b7280;
            transition: all 0.3s ease;
        }

        .fullscreen-toggle-btn:hover {
            background: rgba(59, 130, 246, 0.1);
            transform: scale(1.05);
        }

            .fullscreen-toggle-btn:hover i {
                color: #3b82f6;
            }

    /* ===================== RESPONSIVE DESIGN ===================== */

    @@media (max-width: 768px) {
        .app-header .container-fluid

    {
        padding: 0 0.75rem;
    }

    .sidebar-toggle-btn,
    .fullscreen-toggle-btn {
        width: 38px;
        height: 38px;
    }

        .sidebar-toggle-btn i,
        .fullscreen-toggle-btn i {
            font-size: 1.25rem;
        }

    }


    /* ===================== ADDITIONAL ENHANCEMENTS ===================== */

    /* Logo/Brand Area (se você adicionar) */
    .navbar-brand-custom {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.5rem 1rem;
        border-radius: 10px;
        transition: all 0.3s ease;
        text-decoration: none;
    }

        .navbar-brand-custom:hover {
            background: var(--caa-green-light);
            transform: scale(1.02);
        }

    .brand-icon {
        width: 36px;
        height: 36px;
        background: linear-gradient(135deg, var(--caa-green-main), var(--caa-green-dark));
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.25rem;
        box-shadow: 0 3px 10px rgba(37, 211, 102, 0.25);
    }

    .brand-text {
        font-weight: 700;
        font-size: 1.1rem;
        color: var(--caa-green-dark);
    }

    /* Notification Badge (para futuras notificações) */
    .notification-badge {
        position: absolute;
        top: 8px;
        right: 8px;
        background: #ef4444;
        color: white;
        font-size: 0.7rem;
        font-weight: 600;
        padding: 0.15rem 0.35rem;
        border-radius: 10px;
        box-shadow: 0 2px 6px rgba(239, 68, 68, 0.4);
        animation: pulse 2s ease-in-out infinite;
    }

    @@keyframes pulse {
        0%, 100%

    {
        transform: scale(1);
    }

    50% {
        transform: scale(1.1);
    }

    }

    /* Divider entre grupos de itens */
    .navbar-divider {
        width: 1px;
        height: 30px;
        background: rgba(37, 211, 102, 0.2);
        margin: 0 0.75rem;
    }
</style>

